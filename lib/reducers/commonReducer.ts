import {

} from '../constants/actionTypes';

const defaultState = {
    appName: 'NextjsReduxTemplate'
}

export default (state = defaultState, action) => {
    switch(action.type) {
      default:
        return state;
    }
  }